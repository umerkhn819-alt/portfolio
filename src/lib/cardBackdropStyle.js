/** Inline style for skill/project card photos — deep black with subtle detail (no grey wash). */
export const cardBackdropImageFilter = {
  filter: "brightness(0.5) contrast(1.08) saturate(0.5)",
};
